#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: 0 on success, non-zero on failure
 */
int main(void)
{
	for (char i = "a"; i <= "z"; i++)
	{
		putchar(i);
	}
	putchar("\n");

	return 0;

}
